apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: elpai-openschema-stg

resources:
  - ../../base

patchesStrategicMerge:
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/environment: stg
